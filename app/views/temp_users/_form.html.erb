<%= form_for(@temp_user) do |f| %>
  <% if @temp_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@temp_user.errors.count, "error") %> prohibited this temp_user from being saved:</h2>

      <ul>
      <% @temp_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :namee %><br>
    <%= f.text_area :namee %>
  </div>
  <div class="field">
    <%= f.label :pin %><br>
    <%= f.text_area :pin %>
  </div>
  <div class="field">
    <%= f.label :expiry_at %><br>
    <%= f.datetime_select :expiry_at %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
